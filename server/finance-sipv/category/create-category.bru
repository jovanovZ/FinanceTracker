meta {
  name: create-category
  type: http
  seq: 15
}

post {
  url: {{API_URL}}/category
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Groceries"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
