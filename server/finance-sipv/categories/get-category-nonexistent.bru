meta {
  name: GET - nepostojeci ID
  type: http
  seq: 6
}

get {
  url: {{API_URL}}/category/000000000000000000000000
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

assert {
  res.status: eq 404
}

script:post-response {
  test("Response nije prazan", function() {
    expect(res.body).to.not.be.empty;
  });
}
