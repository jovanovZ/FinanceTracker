meta {
  name: logout
  type: http
  seq: 4
}

post {
  url: {{API_URL}}/auth/logout
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.message: isDefined
}
