meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
  "email":"nejc@nejc.si",
  "password":"nejc"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
