meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{API_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
  "email":"nejc@nejc.si",
  "password":"geslo1234"
  }
}
script:post-response {
  if(res.status === 200) {
    bru.setEnvVar("TOKEN", res.body.token);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
