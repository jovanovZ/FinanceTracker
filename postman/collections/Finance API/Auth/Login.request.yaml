$kind: http-request
name: Login
url: "{{base_url}}/auth/login"
method: POST
body:
  type: json
  content: "{\r

    \  \"email\": \"test@example.com\",\r

    \  \"password\": \"password123\"\r

    }"
scripts:
  - type: afterResponse
    code: "const json = pm.response.json();\r

      if (json.token) {\r

      \    pm.environment.set(\"auth_token\", json.token);\r

      \    console.log(\"Token sačuvan: \" + json.token);\r

      }"
    language: text/javascript
order: 962899307038682
